export const runtime = 'nodejs';

export default function SettingsPage() {
  return (
    <div className="center-stack">
      <h1>Settings</h1>
      <p style={{ color: 'var(--color-text-muted)' }}>
        Settings screen placeholder. Configure workspace preferences.
      </p>
    </div>
  );
}
