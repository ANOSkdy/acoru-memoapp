export const runtime = 'nodejs';

export default function FavoritesPage() {
  return (
    <div className="center-stack">
      <h1>Favorites</h1>
      <p style={{ color: 'var(--color-text-muted)' }}>
        Favorites screen placeholder. Store pinned notes here.
      </p>
    </div>
  );
}
