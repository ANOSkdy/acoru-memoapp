export const runtime = 'nodejs';

export default function NotesPage() {
  return (
    <div className="center-stack">
      <h1>Notes</h1>
      <p style={{ color: 'var(--color-text-muted)' }}>
        Notes screen placeholder. Connect to the notes table when ready.
      </p>
    </div>
  );
}
